import { bbb } from "./bbb";
import { obj } from "./cki";
const cli = require("cac")();
const xxx = "node";
cli.option("--type <type>", "Choose a project type", {
  default: xxx,
});
const parsed = cli.parse();
const qq = `cc`;
console.log(qq, JSON.stringify(parsed, null, 2));

const aaa = adf
console.log("adsf", aaa, bbb());

fun()
function fun() {
  // Implement

}